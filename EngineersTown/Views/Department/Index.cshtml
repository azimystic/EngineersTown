@model IEnumerable<Department>

@{
    ViewData["Title"] = "Departments";
}

<div class="w-full py-6">
    <div class="w-full">
        <div class="bg-white rounded-lg shadow-sm border-0">
            <div class="bg-gradient-to-r from-primary-700 to-primary-600 text-white py-4 px-6 rounded-t-lg">
                <div class="flex justify-between items-center">
                    <h3 class="text-xl font-semibold mb-0">
                        <i class="fas fa-sitemap mr-2"></i>
                        Departments
                    </h3>
                    <a asp-action="Create" class="bg-white text-primary-700 hover:bg-gray-50 px-4 py-2 rounded-md font-medium transition-colors duration-200">
                        <i class="fas fa-plus mr-2"></i> Add Department
                    </a>
                </div>
            </div>

            <div class="p-6">
                @if (Model.Any())
                {
                    <div class="rounded-lg overflow-hidden">
                        <table id="departmentsTable" class="w-full table-auto" style="width:100%">
                            <thead class="bg-gray-800 text-white">
                                <tr>
                                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase tracking-wider">Department Name</th>
                                    <th class="px-6 py-3 text-center text-sm font-semibold uppercase tracking-wider">Actions</th>
                                </tr>
                            </thead>
                            <tbody class="bg-white divide-y divide-gray-200">
                                @foreach (var item in Model)
                                {
                                    <tr class="hover:bg-gray-50 transition-colors duration-200">
                                        <td class="px-6 py-4 font-semibold text-gray-900">@item.Name</td>
                                        <td class="px-6 py-4 text-center">
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-1 rounded-md text-sm transition-colors duration-200" title="Edit">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="text-center py-12">
                        <i class="fas fa-sitemap text-6xl text-gray-400 mb-4"></i>
                        <h4 class="text-xl text-gray-600 mb-3">No departments found</h4>
                        <p class="text-gray-500 mb-6">Get started by adding your first department.</p>
                        <a asp-action="Create" class="bg-primary-600 hover:bg-primary-700 text-white px-6 py-3 rounded-md font-medium transition-colors duration-200">
                            <i class="fas fa-plus mr-2"></i> Add Department
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#departmentsTable').DataTable({
                "pageLength": 10,
                "lengthMenu": [10, 25, 50, 100],
                "order": [[0, "asc"]],
                "dom": '<"flex flex-wrap items-center justify-between mb-4"<"flex-1"l><"flex-1 text-right"f>>rt<"flex flex-wrap items-center justify-between mt-4"<"flex-1"i><"flex-1 text-right"p>>',
                "language": {
                    "search": "<i class='fas fa-search'></i>",
                    "searchPlaceholder": "Search departments...",
                    "lengthMenu": "Show _MENU_ entries",
                    "info": "Showing _START_ to _END_ of _TOTAL_ entries",
                    "paginate": {
                        "previous": "<i class='fas fa-chevron-left'></i>",
                        "next": "<i class='fas fa-chevron-right'></i>"
                    }
                },
                "columnDefs": [
                    { "orderable": false, "targets": 1 }
                ],
                "initComplete": function() {
                    $('.dataTables_filter input')
                        .removeClass()
                        .addClass('px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-primary-500 focus:border-primary-500');
                    $('.dataTables_length select')
                        .removeClass()
                        .addClass('px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-primary-500 focus:border-primary-500');
                }
            });
        });
    </script>

    <style>
        /* DataTables custom styling for Tailwind */
        .dataTables_wrapper .dataTables_paginate .paginate_button {
            padding: 0.5rem 0.75rem !important;
            margin-left: 0.25rem !important;
            border-radius: 0.375rem !important;
            border: 1px solid #d1d5db !important;
            background: white !important;
            color: #374151 !important;
            text-decoration: none !important;
        }

        .dataTables_wrapper .dataTables_paginate .paginate_button.current,
        .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
            background: #2563eb !important;
            color: white !important;
            border: 1px solid #2563eb !important;
        }

        .dataTables_wrapper .dataTables_paginate .paginate_button.disabled {
            color: #9ca3af !important;
            cursor: not-allowed !important;
        }

        .dataTables_wrapper .dataTables_filter label,
        .dataTables_wrapper .dataTables_length label {
            font-weight: 500;
            color: #374151;
        }

        .dataTables_wrapper .dataTables_info {
            color: #6b7280;
            font-size: 0.875rem;
        }
    </style>
}